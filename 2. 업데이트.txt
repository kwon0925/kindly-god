# Kindly 업데이트 내역

**기준일: 2026년 3월 20일**

---

## 1. 프로젝트 구성

- Flutter 웹앱 생성 (kindly_god), GitHub·Vercel 배포 대상
- 의존성: go_router, google_fonts, flutter_riverpod, firebase_core, firebase_auth, google_sign_in
- 테마: app_theme.dart (녹색 계열, Noto Sans KR)
- 라우팅: splash, login, onboarding, home, religion/:id, board, board/:id, board-general, board-general/:id, support, terms

---

## 2. 화면 구성

- **스플래시**: 2초 후 홈으로 이동 (종교·국가 선택 없음)
- **홈**: 랭킹 탭(오늘/이번주/이번달/1년, 종교별/계정별/국가별), 종교별 응원 포인트 그리드, **활동 소식** 섹션, **게시판** 섹션(최근 5개 제목 + 전체보기), 하단 네비(홈/응원하기/활동 소식)
- **계정(사람 아이콘)**: 테스트 유저 A·B·C 선택, 계정 이름 입력, 나의 종교·국가 선택/재선택, Google 로그인
- **응원하기**: 금액 선택(1/10/100/1000/기타) 후 **결제** 버튼으로 포인트 적립
- **활동 소식**: 활동 소식 전용 목록(이미지+요약 3개), 상세 화면 (/board, /board/:id)
- **게시판**: 활동 소식과 별도. 메인에는 최근 5개 제목만 노출, 전체보기 → 게시판 전체 10개 목록·상세 (/board-general, /board-general/:id)
- **약관**: 비영리 아님, 기부금 영수증 대상 아님, 수익 일부 사회공헌 사용 가능 문구

---

## 3. 이미지·에셋

- **assets/images/** 폴더 생성, 1.png~6.png, main.png, main1.png 이동
- **활동 소식**: 1·2·3번 이미지(assets/images/1.png~3.png) 사용
- **메인 팝업**: 접속 시 main.png 다이얼로그 표시, 닫기 버튼으로 종료

---

## 4. 랭킹·포인트

- 랭킹 1·2·3위: 금·은·동 원형 배경 + 👑 아이콘
- **테스트 포인트**: Riverpod(TestPointNotifier)로 메모리 저장, 앱 재시작 시 0부터
- 테스트 유저 A·B·C: 계정 ID, **계정 이름** 사용자 입력 가능 → 계정별 랭킹에 반영
- 결제 시 포인트: 선택한 유저·종교·국가에 동시 적립
- **종교별 그리드**: 포인트 많은 순서로 정렬 표시

---

## 5. 종교·국가

- **종교별 상징(마크)**: 기독교 ✝, 이슬람 ☪, 힌두교 ॐ, 불교 ☸, 시크교 ☬, 유대교 ✡, 도교 ☯, 유교 文, 신토 ⛩, 기타 ◆
- Religion.symbol(id)로 카드·상세 화면에 표시
- 나의 종교·국가는 계정 다이얼로그에서 선택/재선택

---

## 6. Firebase

- **firebase_config.dart**: kindly-god 프로젝트 설정(apiKey, authDomain, projectId 등)
- **Google 로그인(웹)**: googleWebClientId, web/index.html meta 태그(google-signin-client_id) 설정
- Authentication(Google·Apple 사용 설정), 승인된 도메인(localhost 등)
- 로그인 실패 시 웹용 OAuth 클라이언트 ID 확인 안내 (docs/FIREBASE_SETUP.md 3-1)

---

## 7. UI·UX 수정

- 계정 다이얼로그: **오버플로 방지** (ConstrainedBox maxHeight, Wrap으로 유저 칩 줄바꿈), 패딩·폰트 축소
- 달러 버튼: 금액만 선택, **결제 버튼** 누를 때만 포인트 적립
- ActionChip → FilterChip 변경(기타 칩 selected 지원)

---

## 8. 파일 구조 요약

- lib/config: app_theme, routes, firebase_config
- lib/models: religion, country, board_post, ranking
- lib/state: test_point_state, test_point_provider
- lib/services: auth_service
- lib/widgets: responsive_layout, ranking_tabs, religion_card, home_religion_grid, home_activity_section, **home_board_section**, main_popup_overlay, account_dialog
- lib/screens: splash, login, onboarding, home, religion_detail, board, board_detail, **board_general**, **board_general_detail**, support, terms
- lib/data: dummy_board (활동 소식 3개), **dummy_board_general** (게시판 10개, 최근 5개 메인 노출)
- docs: FIREBASE_SETUP.md

---

## 9. 게시판 (활동 소식과 분리)

- **활동 소식**: dummy_board.dart (3건), 이미지+요약, /board, /board/:id
- **게시판**: dummy_board_general.dart (10건), 제목·요약·본문, /board-general, /board-general/:id
- 메인: 활동 소식 아래 **게시판** 섹션에 최근 5개 제목만 노출, 전체보기 → 게시판 전체 목록
- getLatestBoardGeneralPosts(5)로 최신 순 5개만 메인에 표시

---

## 10. 테스트 흐름

1. 앱 실행 → 스플래시 후 홈
2. 사람 아이콘 → 유저 A/B/C 선택, 계정 이름 입력, 종교·국가 선택
3. 응원하기 → 금액 선택 → 결제 버튼 → 포인트 적립
4. 홈에서 랭킹·종교 그리드(포인트 순) 확인
5. 게시판: 메인에서 최근 5개 제목 확인 → 전체보기 → 게시글 10개 목록·상세

---

## 11. 2026년 3월 15일 업데이트

### 11-1. 랭킹·포인트 (Firestore 실시간)
- **Google 로그인 사용자** 포인트: 계정별뿐 아니라 **종교별·국가별** 랭킹 및 메인 종교 그리드에 반영
- UserProfileRepository: `getReligionRankingPoints()`, `getCountryRankingPoints()` 추가 (users 컬렉션 집계)
- **StreamProvider** 사용으로 Firestore 변경 시 자동 반영 (religionPointsFromServerProvider, countryPointsFromServerProvider)
- ranking_tabs, home_religion_grid: 로컬(테스트) + 서버(Firestore) 포인트 병합 표시

### 11-2. 메인 화면 종교 그리드
- **상위 8개** 종교만 포인트 순으로 표시
- **전체보기** 버튼 → `/religions` (AllReligionsScreen)에서 11개 전체 표시
- HomeReligionGrid: limit 파라미터(기본 8), 전체보기 링크

### 11-3. 게시판 Firebase 연동
- **활동 소식**: 기존 하드코딩 유지 (관리자용, dummy_board)
- **게시판**: Firestore `posts` 컬렉션 연동, **댓글** 서브컬렉션
- 모델: community_post.dart (제목·본문·작성자·익명·commentCount), community_comment.dart
- BoardRepository: 게시글/댓글 CRUD, 실시간 스트림
- **글쓰기**: 로그인 사용자만, **아이디명** or **익명** 체크박스 선택
- 게시글·댓글 목록에 표시 이름: 익명 체크 시 "익명", 미체크 시 서버 저장 아이디명
- dummy_board_general.dart 삭제, 라우트: /board-write (글쓰기)

### 11-4. UI·버튼·레이아웃 수정
- **게시글 작성**: 게시하기 버튼 **하단 고정** (board_write_form.dart 분리)
- **게시하기 버튼**: Teal 색상으로 구분 (board_button_style.dart)
- **활동 상세** 이미지: PC에서 잘리지 않도록 BoxFit.contain, PC 시 높이 360 (activity_detail_hero.dart)
- **랭킹 탭**: 이름·포인트 글씨 PC에서 잘림 방지 (overflow: ellipsis, 포인트 영역 56px)

### 11-5. 파일 구조 추가
- lib/models: community_post, community_comment
- lib/repository: board_repository
- lib/state: board_provider
- lib/widgets: activity_detail_hero, board_write_form, board_button_style
- lib/screens: board_write_screen, all_religions_screen
- Firestore 규칙: posts(읽기 전체, 생성 로그인, 삭제 본인), posts/{id}/comments 동일

---

## 12. 2026년 3월 16일 업데이트

### 12-1. 빌드·푸시·배포
- 로컬 `flutter build web` 후 `build/web` 커밋 → GitHub main 푸시 → Vercel 자동 배포
- Firebase CLI: 프로젝트 전환(fastapi-js → kindly-god), `.firebaserc` default: kindly-god
- Firestore 보안 규칙: `npx firebase use kindly-god` 후 `npx firebase deploy --only firestore:rules` 로 kindly-god 프로젝트에 배포 완료

### 12-2. Vercel 배포 후 channel-error (Firebase 연동 실패)
- 증상: 로컬은 정상, 배포 사이트에서 랭킹 무한 로딩, 아이디 중복 검사 무반응
- 콘솔 에러: `PlatformException(channel-error, Unable to establish connection on channel., null, null)` (Auth/Firestore JS 채널 미연결)
- 원인: Flutter 웹 PWA 서비스워커가 예전 빌드(main.dart.js 등)를 캐시해 새 배포가 반영되지 않음
- 확인: Firebase Auth 승인된 도메인에 kindly-god.vercel.app 이미 등록됨 → 도메인 문제 아님
- 해결(당시): 시크릿 모드 또는 브라우저에서 해당 사이트 서비스워커 Unregister + Clear site data 후 재접속 시 정상

### 12-3. PWA 서비스워커 비활성화 (캐시 문제 근본 해결)
- 웹 빌드 시 `--pwa-strategy=none` 적용 → 서비스워커 미생성
- 명령: `flutter build web --release --pwa-strategy=none`
- 효과: 배포 시 항상 최신 JS 로드, 사용자에게 캐시 삭제 요청 불필요. 기존 방문자도 1~2회 재접속 시 최신 버전으로 전환

### 12-4. 빌드·푸시·배포 재실행
- `flutter clean` → `flutter pub get` → `flutter build web --release --pwa-strategy=none`
- `git add -f build/web` → commit → push origin main (Vercel 자동 배포)

### 12-5. 천국의 땅 분양 (가칭) — 삭제함, 나중에 적용용 코딩만 보관
- 홈 화면 랭킹 아래에 임시 섹션(HomeHeavenLandSection) 추가 후 로컬 테스트용으로만 사용
- **현재 앱에서는 삭제됨.** 아래 코딩 내용은 나중에 판매/PDF 증서 기능 구현 시 참고용으로만 적용.

---

#### [나중에 적용] 천국의 땅 섹션 위젯 파일

**파일 생성: lib/widgets/home_heaven_land_section.dart**

```dart
import 'package:flutter/material.dart';

/// 홈 화면 상단에 임시로 노출하는 "천국의 땅" 섹션.
/// - 당장은 로컬 테스트용으로만 사용.
class HomeHeavenLandSection extends StatelessWidget {
  const HomeHeavenLandSection({super.key});

  @override
  Widget build(BuildContext context) {
    final theme = Theme.of(context);
    final colorScheme = theme.colorScheme;

    return Card(
      margin: const EdgeInsets.only(top: 16),
      shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(16)),
      elevation: 3,
      child: Padding(
        padding: const EdgeInsets.all(18),
        child: Row(
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            Icon(
              Icons.cloud_queue_rounded,
              size: 40,
              color: colorScheme.primary,
            ),
            const SizedBox(width: 16),
            Expanded(
              child: Column(
                crossAxisAlignment: CrossAxisAlignment.start,
                mainAxisSize: MainAxisSize.min,
                children: [
                  Text(
                    '천국의 땅 분양 (임시)',
                    style: theme.textTheme.titleMedium?.copyWith(
                      fontWeight: FontWeight.w700,
                    ),
                  ),
                  const SizedBox(height: 6),
                  Text(
                    '나만의 천국 좌표를 소유하고 PDF 증서를 발급받는 상품입니다. '
                    '현재는 콘셉트 테스트 단계입니다.',
                    style: theme.textTheme.bodySmall?.copyWith(
                      color: Colors.grey.shade700,
                    ),
                  ),
                  const SizedBox(height: 10),
                  Wrap(
                    spacing: 8,
                    runSpacing: 4,
                    children: const [
                      _TagChip(label: 'PDF 소유 증서'),
                      _TagChip(label: '한정 좌표'),
                      _TagChip(label: '닉네임 기재'),
                    ],
                  ),
                  const SizedBox(height: 14),
                  Align(
                    alignment: Alignment.centerRight,
                    child: FilledButton(
                      onPressed: () {
                        showDialog<void>(
                          context: context,
                          builder: (ctx) => AlertDialog(
                            title: const Text('천국의 땅 (테스트용)'),
                            content: const Text(
                              '여기는 임시로 배치한 섹션입니다.\n\n'
                              '실제 판매 화면과 PDF 증서 발급 플로우는 '
                              '향후 별도 화면(/heaven-lands 등)으로 구현할 예정입니다.',
                            ),
                            actions: [
                              TextButton(
                                onPressed: () => Navigator.of(ctx).pop(),
                                child: const Text('확인'),
                              ),
                            ],
                          ),
                        );
                      },
                      child: const Text('천국의 땅 콘셉트 보기'),
                    ),
                  ),
                ],
              ),
            ),
          ],
        ),
      ),
    );
  }
}

class _TagChip extends StatelessWidget {
  final String label;

  const _TagChip({required this.label});

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: const EdgeInsets.symmetric(horizontal: 10, vertical: 4),
      decoration: BoxDecoration(
        color: Colors.grey.shade100,
        borderRadius: BorderRadius.circular(999),
        border: Border.all(color: Colors.grey.shade300),
      ),
      child: Text(
        label,
        style: Theme.of(context).textTheme.labelSmall?.copyWith(
              color: Colors.grey.shade800,
              fontWeight: FontWeight.w500,
            ),
      ),
    );
  }
}
```

**홈 화면에 넣을 때 (lib/screens/home_screen.dart):**
- import 추가: `import '../widgets/home_heaven_land_section.dart';`
- Column children 안, `const RankingTabs(),` 다음 줄에: `const HomeHeavenLandSection(),`

---

*위 내용은 2025-03-11 기준 작성 후, 2026-03-15·2026-03-16 업데이트를 반영한 것입니다.*

---

## 13. 2026년 3월 17일 업데이트

### 13-1. 홈 UI 개편: “랭킹” + “종교별 응원 포인트” 통합
- 기존 홈의 `RankingTabs`(종교별·계정별 랭킹) + `HomeReligionGrid`(종교별 응원 포인트) 구조를 **하나의 통합 섹션**으로 재구성
- 홈 화면은 `HomeRankingSection` 단일 섹션으로 운영 (유지보수/확장 용이하도록 파일 분리)

### 13-2. 기간/타입 탭 개편
- 기간 탭: **오늘 / 이번주 / 이번달 / 전체** (기존 “1년” → “전체”로 변경)
- 타입 탭: **종교 / 계정 / 국가**
- `lib/models/ranking.dart`
  - `RankingPeriod.year` → `RankingPeriod.all`
  - `rankingPeriodLabels`, `rankingTypeLabels` 상수 추가

### 13-3. 상위 1·2·3위 메달 오버레이
- 1·2·3위 카드에 **순위 텍스트 + 메달(🥇🥈🥉)** 을 이미지/카드 상단에 겹쳐 표시
- 공용 위젯 분리: `lib/widgets/rank_medal_overlay.dart`
- `lib/widgets/religion_card.dart`: `rank` 파라미터 추가 후 오버레이 적용

### 13-4. 국가 카드 + 국기 표시
- 국가 탭에서 국가명/포인트와 함께 **국기 이미지 노출**
- `assets/images/flags/` 폴더의 파일명 suffix(`_kr`, `_jp`, …)를 기준으로 국가 코드 매핑
- `lib/models/country.dart`: `Country.flagAssetPath(countryId)` 추가 (KR/JP/US/CN/IN/GB/DE/FR/BR/ID 매핑)
- `lib/widgets/country_card.dart`: 국기 이미지 크기 확대(비율 유지 `BoxFit.contain`)

### 13-5. 국기 asset 404 해결
- Flutter Web에서 국기 파일이 404로 로드 실패하던 문제 해결
- `pubspec.yaml`에 국기 폴더를 명시적으로 등록:
  - `assets/images/flags/`

### 13-6. 종교/국가 노출 정책 개선
- 종교 탭: 포인트가 0이어도 **항상 기본 종교 목록을 포함**하여 상위 8개 노출 + 전체보기
- 국가 탭: 종교 탭과 동일하게 **0포인트 포함** 상위 8개 노출
- 동점일 때는 “랜덤 노출” 요구를 만족하되 UI가 계속 바뀌지 않도록:
  - **하루(UTC) 단위 시드 기반**으로 동점 그룹만 셔플 (같은 날에는 고정, 날짜가 바뀌면 변화)

### 13-7. 계정 탭에 국가·종교 표시
- 계정 랭킹 리스트에서 아이디(표시명) 아래에 **국가 · 종교**를 `subtitle`로 표시
- 서버 계정: `UserProfile.religionId`/`countryId` → `defaultReligions`/`defaultCountries`에서 이름 매핑
- 로컬 테스트 계정(A/B/C): 종교/국가 정보가 없으므로 미표시

### 13-8. 구글 로그인 계정 전환(로그아웃 후 B 로그인 시 A 정보 잔상) 문제 해결
- 문제: A로 로그인 후 로그아웃 → B로 로그인했는데도 UI에 A의 “비활성화/잠금” 상태가 남아 보이는 현상
- 원인: `currentUserProfileProvider`가 `AuthService.currentUser`(스냅샷) 기반으로 구성되어 **Auth 상태 변경(A→null→B)** 에 따라 스트림/캐시가 자동으로 재구독되지 않을 수 있음
- 해결:
  - Auth 상태 스트림을 Provider 그래프에 연결 (`authStateChanges` 기반 `authUserProvider` 추가)
  - `currentUserProfileProvider`가 `authUserProvider`에 의존하도록 변경해 **계정 변경 시 프로필 스트림 자동 갱신**
  - 로그아웃 시 관련 Provider들을 `invalidate()` 하여 잔상/캐시 제거
- 결과: 계정 전환 시 **정상적으로 B 계정 기준으로 화면이 갱신됨** (테스트 완료)

---

## 14. 2026년 3월 18일 업데이트

### 14-1. 통합 게시판 스키마 확장 (활동소식/자유게시판 통합 운영)
- Firestore `posts` 컬렉션에 필드 추가:
  - `category`: `'news' | 'board'`
  - `religion`: 가입 시 선택한 종교 id (예: `judaism`, `buddhism` 등)
  - (옵션) `likeCount` 기본값 포함
- `lib/models/community_post.dart`:
  - `PostCategory` 상수 추가
  - `category`, `religion`, `likeCount` 모델 반영 및 직렬화(from/to Firestore) 반영

### 14-2. 공통 목록 위젯 도입: PostListWidget
- `lib/widgets/post_list_widget.dart` 추가:
  - `PostListWidget(String? religion, String? category)` 형태로 재사용
  - Stream 기반 실시간 갱신 + `orderBy(createdAt)` + `limit` 적용
  - 홈(임베드)용 `scrollable=false` / 전체화면용 `scrollable=true` 지원
- `lib/models/post_list_filter.dart`, `lib/state/board_provider.dart`:
  - 필터 파라미터 + `filteredBoardPostsProvider`(family) 추가

### 14-3. “가입 종교에서만 글쓰기 가능” 정책 적용 (UI + Firestore Rules)
- 앱 정책: 유저는 **가입 시 선택한 종교 게시판에서만 글 작성 가능**
- UI:
  - `lib/screens/religion_detail_screen.dart`: 내 `religionId`와 화면 종교가 같을 때만 “글쓰기” 버튼 노출
  - `lib/screens/board_general_screen.dart`: 내 `religionId`가 있을 때만 “글쓰기” 버튼 노출 (내 종교로만 진입)
- 작성 로직:
  - `lib/widgets/board_write_form.dart`: 게시글 저장 시 `users/{uid}.religionId`를 읽어 `posts.religion`에 강제 저장
- 보안:
  - `firestore.rules`: `posts` create 시 `request.resource.data.religion == users/{uid}.religionId` 조건 추가로 서버단 강제

### 14-4. 홈/게시판 화면 연동 정리
- 홈:
  - `HomeActivitySection` → `PostListWidget(category: news, limit: 5, horizontal)`
  - `HomeBoardSection` → `PostListWidget(category: board, limit: 5)`
- 게시판 전체보기:
  - `BoardGeneralScreen` → `PostListWidget(category: board, scrollable: true)`

### 14-5. Firestore 인덱스/배포 및 Hosting 배포 정리
- Firestore:
  - `firestore.indexes.json` 추가 및 `firebase.json`에 indexes 연결
  - `firebase deploy --only firestore:rules,firestore:indexes`로 배포 완료
- Hosting:
  - `firebase.json`에 hosting 설정 추가(`public: build/web`, SPA rewrite 포함)
  - `flutter build web --release --pwa-strategy=none` 후 `firebase deploy --only hosting` 배포 완료
  - 접속 주소: `https://kindly-god.web.app`

### 14-6. 버전 표시 (배포 버전 확인용)
- 메인 AppBar 제목 좌측에 빨간색 버전 라벨 표시: `v1.0.0`
- 코드 분리:
  - `lib/config/app_version.dart` (버전 문자열)
  - `lib/widgets/version_badge.dart` (표시 위젯)

---

## 15. 2026년 3월 20일 업데이트

### 15-1. 어드민(테스트) 접속
- 계정 팝업·로그인 전용 팝업에 **어드민 접속** 버튼 추가
- Firebase Auth **익명 로그인** 후 `users/{uid}`에 `role: 'admin'` 기록 (`grantAdminRole`)
- 코드 분리: `lib/auth/admin/admin_role.dart`, `lib/auth/admin/admin_auth_service.dart`, `AuthService.signInAsAdmin()`
- 익명 로그인 미활성화 시 콘솔에서 Anonymous 제공자 켜기 안내

### 15-2. 어드민 권한 (게시판)
- Firestore Rules: `isAdmin()` 기준으로 게시글·댓글 삭제·게시글 작성(종교 제한 우회) 등 허용
- UI: `board_general_screen`, `religion_detail_screen`에서 어드민은 **글쓰기 FAB** 노출, `board_write_form`·상세 화면에서 삭제/작성 동작 정리
- Rules 배포: `firebase deploy --only firestore:rules`로 반영

### 15-3. 랭킹 기간 탭 + 포인트 원장(`pointEvents`)
- 메인 랭킹: **오늘 / 이번주 / 이번달 / 전체**를 서버 데이터 기준으로 집계
  - 오늘: 로컬 **0시~24시**
  - 이번주: **월요일 0시 ~ 다음 주 월요일 0시**
  - 이번달: **당월 1일 ~ 다음 달 1일**
  - 전체: 기존 `users` 누적 집계 유지
- 결제 적립·광고 적립 시 `pointEvents` 문서에 **타임스탬프·uid·종교·국가·포인트·유형(payment/adWatch)·amountUsd** 기록
- Provider: `accountRankingByPeriodProvider`, `religionPointsByPeriodProvider`, `countryPointsByPeriodProvider`
- `home_ranking_section`: 기간별 provider로 표시 (과거 데이터는 원장 없으면 기간 탭에만 반영 안 될 수 있음)

### 15-4. Firestore Rules (`pointEvents`)
- `pointEvents`: 읽기 공개(랭킹), 생성은 로그인 사용자·본인 `uid`만, 수정/삭제 불가

### 15-5. 버전·배포
- 앱 버전: `pubspec.yaml` **1.0.4+4**, 표시 라벨 **v1.0.4** (`app_version.dart`)
- 웹 빌드: `flutter build web --release --pwa-strategy=none`
- Firebase Hosting 배포 완료: `https://kindly-god.web.app`

### 15-6. 기타 수정
- 기간 쿼리용 `_periodEventSnapshot`: Firestore `Query` 타입 명시로 웹 빌드 오류 수정

---

## 16. 2026년 3월 26일 업데이트

### 16-1. 글로벌 다국어(l10n) 적용 범위 확장
- `l10n.yaml` + `lib/l10n/*.arb` 기반 10개 언어(ko/en/zh/ja/es/fr/de/ru/pt/ar) 운영 유지
- 메인/랭킹/활동/게시판/종교 상세/천국 영토 구매·완료 화면의 하드코딩 문자열을 다국어 키로 정리
- 언어 변경 시 `LocaleProvider`를 통해 앱 로케일 즉시 반영 (계정 팝업에서 언제든 변경 가능)

### 16-2. 천국 영토 구매 탭 번역 누락 보완
- 원인: 천국 구매/완료 화면 신규 키가 일부 언어 ARB에 누락되어 한국어/영어로 폴백되던 상태
- 조치: `app_zh.arb`, `app_ja.arb`, `app_es.arb`, `app_fr.arb`, `app_de.arb`, `app_ru.arb`, `app_pt.arb`, `app_ar.arb`에 누락 키(구매 제목, 입력 라벨/힌트, 안내문, 완료 문구 등) 추가
- 결과: 천국 영토 구매 탭이 선택 언어 기준으로 정상 표시

### 16-3. 게시판 번역 기능(ML Kit) 실제 연동
- 의존성 추가: `google_mlkit_translation`
- `lib/services/translation_service.dart`:
  - `OnDeviceTranslatorModelManager`로 언어 모델 존재 여부 확인 및 다운로드
  - `OnDeviceTranslator`로 원문(`ko`) -> 사용자 선택 언어 변환
  - 지원 언어 매핑(ko/en/zh/ja/es/fr/de/ru/pt/ar) 및 실패 시 에러 메시지 처리
- `TranslateButton`은 기존 호출 구조 유지, 서비스 구현만 교체해 게시글 번역 동작 연결
- 참고: ML Kit은 네이티브(Android/iOS) 중심이며 웹에서는 미지원 동작 유지

### 16-4. 빌드/배포
- `flutter gen-l10n` 재생성
- `flutter build web --release` 빌드 성공
- Firebase Hosting 재배포 완료: `https://kindly-god.web.app`

