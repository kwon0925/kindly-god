{
  "version": 2,
  "buildCommand": "if [ ! -d \"flutter\" ]; then git clone https://github.com/flutter/flutter.git -b stable --depth 1; fi && ./flutter/bin/flutter pub get && ./flutter/bin/flutter build web --release --no-tree-shake-icons --no-minify",
  "outputDirectory": "build/web",
  "rewrites": [
    { "source": "/", "destination": "/index.html" },
    { "source": "^/((?!.*\\.).*)$", "destination": "/index.html" }
  ]
}
